{
  "name": "be-app",
  "type": "module",
  "scripts": {
    "clear": "rm -rf dist dist-server node_modules .wrangler pnpm-lock.yaml",
    "dev": "vite",
    "build": "vite build && vite build --ssr",
    "preview": "$npm_execpath run build && wrangler dev --env production dist-server/index.js",
    "deploy-staging": "$npm_execpath run build && wrangler deploy --env staging dist-server/index.js",
    "deploy": "$npm_execpath run build && wrangler deploy --env production dist-server/index.js",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.19.5",
    "@scalar/hono-api-reference": "^0.8.5",
    "hono": "^4.7.7",
    "path": "^0.12.7",
    "url": "^0.11.4",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^0.1.15",
    "@hono/vite-build": "^1.5.0",
    "vite": "^6.1.1",
    "vite-plugin-ssr-hot-reload": "^0.4.1",
    "wrangler": "^4.4.0"
  }
}
